	.assume	adl=1

	.section	.text
	.global	__sor
	.type	__sor, @function

.if PREFER_OS_CRT

__sor := 000234h

.else

__sor:
	push	af
	ld	a, h
	or	a, b
	ld	h, a
	ld	a, l
	or	a, c
	ld	l, a
	pop	af
	ret

.endif
