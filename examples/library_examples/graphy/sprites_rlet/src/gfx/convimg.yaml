palettes:
  - name: global_palette
    fixed-entries:
      - color: {index: 0, r: 255, g: 255, b: 255}
    images: automatic

converts:
  - name: sprites
    palette: global_palette
    transparent-color-index: 0
    style: rlet
    width-and-height: swap
    flip-x: true
    rotate: 90
    images:
      - ubuntu.png

outputs:
  - type: c
    include-file: gfy.h
    palettes:
      - global_palette
    converts:
      - sprites
